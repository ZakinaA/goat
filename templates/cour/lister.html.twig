<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES COURS</br>
		<table>
		{% for e in pCours %}
		<tr>
			<td><a href="{{ path('courConsulter', { 'id': e.id }) }}">{{ e.libelle }}</a></td>
		
			
            {% else %}
                <tr>
                <td>Aucun cour n'a été trouvé.</td>
            </tr>
            {% endfor %}
        </table>
    </body>
</html>