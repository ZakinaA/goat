<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES ELEVES</br>
		<table>
		{% for e in pEleves %}
		<tr>
			<td><a href="{{ path('eleveConsulter', { 'id': e.id }) }}">{{ e.nom }}</a></td>
			<td><a href="{{ path('eleveConsulter', { 'id': e.id }) }}">{{ e.prenom }}</a></td>
			
            {% else %}
                <tr>
                <td>Aucun eleve n'a été trouvé.</td>
            </tr>
            {% endfor %}
        </table>
    </body>
</html>