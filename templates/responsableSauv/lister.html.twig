<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES RESPONSABLE</br>
		<table>
		{% for e in pResponsables %}
		<tr>
			
			<td>{{ e.prenom }}</a></td>
            <td>{{ e.adresse }}</a></td>
            <td>{{ e.ville }}</a></td>
            <td>{{ e.code_postal }}</a></td>
            <td>{{ e.email }}</a></td>
            <td>{{ e.tel1 }}</a></td>
	{% else %}
		<tr>
		<td>Aucun reponsable n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	 
    </body>
</html>